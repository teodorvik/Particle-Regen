function [answer] = isTouched(particle, ball, radius)
    
    dist = abs(particle - ball);
    if ( dist < radius) 
        % Räkna ut riktningen vart de ska gå med hjälp 
        % av normalen. Sätt sedan velocity med 
        answer = 1;
    else
        answer =  0;
    end
end